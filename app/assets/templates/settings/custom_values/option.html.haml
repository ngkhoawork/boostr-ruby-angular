.row
  .col-xs-10
    .title{ editable: { text: 'option.name' }, onaftersave: 'updateOption(option)', 'e-name': 'name' }
      {{ option.name }}

  %a.delete{ href: '', ng: { click: 'deleteOption(option)', if: '!option.locked' } }
    = inline_svg 'icons/trash.svg'
