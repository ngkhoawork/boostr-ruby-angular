#pipeline-split-report.z-styles
	.container-fluid
		.row.mb20
			.reports-header
				.header-cell.main-title
					Pipeline Split Adjusted
				.header-cell.filters-block
					%table
						%tr.filter-title
							%td Team
							%td Seller
							%td Stage
							%td Status
							%td
								%span.reset-filter{'ng-click': 'resetFilter()'}
									Reset
						%tr
							%td
								%tree-dropdown{ class:"tree-dropdown team-list", data:"teams", selected: 'filter.team', 'ng-class': '{"empty": filter.team.name === "All"}'}

							%td
								%ul.filter-item
									%li{ dropdown: true }
										%a{ href: '', dropdown: { toggle: true }, 'ng-class': '{"empty": filter.seller.name === "All"}' }
											{{(filter.seller.first_name && filter.seller.first_name + ' ' + filter.seller.last_name) || 'All'}}
											%span.caret
										%ul.dropdown-menu
											%li{ ng: { repeat: 'seller in sellers', class: '{active: seller.id === filter.seller.id}' } }
												%a{ href: '', ng: { click: 'setFilter("seller", seller)' } }
													{{ seller.first_name + ' ' + seller.last_name}}

							%td
								%ul.filter-item
									%li{ dropdown: true }
										%a{ href: '', dropdown: { toggle: true }, 'ng-class': '{"empty": filter.stage.name === "All"}' }
											{{filter.stage.name}} {{isNumber(filter.stage.probability) ? filter.stage.probability + '%' : ''}}
											%span.caret
										%ul.dropdown-menu
											%li{ ng: { repeat: 'stage in stages', class: '{active: stage.name === filter.stage.name}' } }
												%a{ href: '', ng: { click: 'setFilter("stage", stage)' } }
													{{stage.name}} {{isNumber(stage.probability) ? stage.probability + '%' : ''}}

							%td
								%ul.filter-item
									%li{ dropdown: true }
										%a{ href: '', dropdown: { toggle: true }, 'ng-class': '{"empty": filter.status.name === "All"}' }
											{{filter.status.name}}
											%span.caret
										%ul.dropdown-menu
											%li{ ng: { repeat: 'status in statuses', class: '{active: status.name === filter.status.name}' } }
												%a{ href: '', ng: { click: 'setFilter("status", status)' } }
													{{ status.name }}

				.header-cell
					.pull-right
						%download-button{'ng-click': 'export()'}
							Export

		.row
			.col-xs-12
				.block
					%div.table-wrapper
						%table.table.table-hover
							%thead
								%tr
									%th Deal ID
									%th Name
									%th Advertiser
									%th Agency
									%th Team Member
									%th Split %
									%th Budget
									%th Stage
									%th Probability
									%th Type
									%th Source
									%th Next Steps
									%th Start Date
									%th End Date
									%th Created Date
									%th Closed Date
							%tbody
								%tr
									%td 651
									%td Q4-Q1 Clearance Retargeting
									%td Best Buy
									%td
									%td Tom Seller
									%td 50%
									%td 750000
									%td Discuss Requirements
									%td 25
									%td
									%td
									%td
									%td 11/15/2016
									%td 1/31/2017
									%td 10/19/2016
									%td
								%tr
									%td 630
									%td Back to Future Lace Ups
									%td Nike, Inc.
									%td
									%td Jen Seller
									%td 75%
									%td 7500
									%td Discuss Requirements
									%td 25
									%td
									%td
									%td
									%td 10/10/2016
									%td 12/24/2016
									%td 10/11/2016
									%td
